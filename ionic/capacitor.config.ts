import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.caredforinterview',
  appName: 'caredforinterview',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
